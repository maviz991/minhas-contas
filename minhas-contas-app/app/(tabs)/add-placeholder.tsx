import { View } from 'react-native';

// Esta tela nunca será mostrada, pois o clique na aba será interceptado.
// Criamos o arquivo apenas para que o Expo Router renderize a aba.
export default function AddModalPlaceholder() {
  return null;
}
